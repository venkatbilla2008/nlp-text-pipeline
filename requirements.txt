# ============================================================================
# requirements.txt - Numpy/spaCy Compatibility
# ============================================================================

# Core dependencies
streamlit>=1.28.0
pandas>=2.0.0

# ULTRA-FAST ANALYTICS
polars>=0.19.0  # 10-50x faster than pandas
pyarrow>=12.0.0  # Fast columnar format
duckdb>=0.9.0  # SQL analytics engine

# CRITICAL: Pin numpy version for spaCy compatibility
numpy==1.24.3

# File handling
openpyxl>=3.1.0
pyarrow>=12.0.0
python-dateutil>=2.8.0

# Sentiment Analysis
vaderSentiment>=3.3.2
textblob>=0.17.0
nltk>=3.8.0

# spaCy for NER-based PII Detection
# IMPORTANT: Install spaCy before the model
spacy>=3.7.0,<3.8.0
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl

# ============================================================================
# CRITICAL FIX: 
# 
# Issue: "numpy.dtype size changed" error
# Cause: numpy 2.x is incompatible with spaCy 3.7.x
# Solution: Use numpy 1.24.3 which is compatible with both spaCy and Streamlit
# ============================================================================
